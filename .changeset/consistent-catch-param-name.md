---
"eslint-plugin-llm-core": minor
---

Add `consistent-catch-param-name` rule to enforce consistent naming for catch clause parameters (default: `"error"`). LLMs frequently mix naming conventions (`e`, `err`, `error`, `ex`) in the same codebase.
