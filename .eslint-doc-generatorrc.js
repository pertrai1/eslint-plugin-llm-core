/** @type {import('eslint-doc-generator').GenerateOptions} */
const config = {
  configEmoji: [
    ["complexity", "🧮"],
    ["best-practices", "🏆"],
    ["hygiene", "🧹"],
  ],
  ignoreConfig: ["bestPractices"],
};

module.exports = config;
